{
    "name": "project",
    "private": true,
    "scripts": {
        "start": "node src",
        "debug": "node --inspect-brk src",
        "test": "node src/tests",
        "test-debug": "node --inspect-brk src/tests",
        "coverage": "nyc --extension .ts npm test",
        "lint": "eslint src/**/*.ts"
    },
    "dependencies": {
        "backend-base": ">1.0.0"
    }
}
